fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
alembic==1.13.2
psycopg[binary]==3.2.2

# Auth
pwdlib[argon2]==0.2.1
pyjwt==2.9.0
python-multipart==0.0.9
pydantic[email]==2.9.2

# Website hosting (Phase 4): Docker SDK to build/run isolated
# containers for dynamic deployments, httpx to reverse-proxy requests
# to them.
docker==7.1.0
httpx==0.27.2

# Admin dashboard (Phase 5): safe OS-level metrics (CPU/RAM/disk/
# network) for the server health card and history graphs.
psutil==6.0.0
