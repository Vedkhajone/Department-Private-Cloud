# Vite auto-loads this file and exposes any VITE_-prefixed variable
# to client code via import.meta.env. Used by components/Footer.tsx
# to show the real app version instead of a hardcoded one. Keep this
# in sync with the "version" field in package.json.
VITE_APP_VERSION=0.4.0
